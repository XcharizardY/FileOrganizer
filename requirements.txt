PyQt6>=6.5.0

# The following are used by the app (dashboard bridge, startup registration,
# logging) but are part of the Python standard library and do not need to
# be installed separately: sqlite3, winreg (Windows only), logging, threading.
